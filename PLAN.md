# תוכנית: בונה-חבילות חכם למוצרי חשמל (AI) + פאנל ניהול — "Premium Electric"

## Context
עסק חדש: דף נחיתה לקמעונאי מוצרי חשמל עם **בונה-חבילות חכם (AI)**. הלקוח בוחר תקציב +
העדפת רמת-מותג + סוגי מכשירים → ה-AI ממליץ על מוצרים בתוך התקציב + עצת שדרוג ("ב-₪X
יותר תקבל דגם טוב יותר") → מציג מחיר פר-מוצר + סה"כ חבילה. בנוסף פאנל ניהול לקליטת
מוצרים (Excel/PDF/Word/אתר) → סקירה → בחירה ל-DB, עם שליפת מחיר-זאף היברידית.

**תמחור:** מוצר בודד = מחיר זאף הנמוך × 0.95 (5%- ). חבילה = סכום(זאף הנמוך) × 0.90 (10%-).
החבילה תמיד עדיפה מקנייה בודדת → תמריץ חזק.

## החלטות המשתמש
- **מחיר זאף:** היברידי — שליפה אוטומטית + אישור/תיקון של המנהל; הזנה ידנית כ-fallback תמיד.
- **בסיס:** לבנות על הדמו הקיים `~/Private/premium-electric` (React 18 + Vite + Tailwind +
  Framer Motion + Lucide, RTL מלא, פלטת זהב+שחור; כבר יש ProductCard/Categories/Hero/
  Navbar/AIChatBot/CompareBar + tailwind tokens).
- **Stack:** React+Vite+Tailwind (פרונט) + Cloudflare Pages Functions (backend, כמו
  fire-safety-landing שיש לו `functions/`) + Firebase Firestore (DB מוצרים) + Storage (העלאות).
- **AI:** Groq כברירת מחדל (זול/מוכח); Claude אופציה לשדרוג חילוץ-מסמכים מבולגנים בלבד.

## עיצוב (מתוך /ui-ux-pro-max)
- **תבנית:** AI-Driven configurator — בונה-החבילות הוא ה"גיבור" של הדף.
- **סגנון:** פרמיום כהה + אקסנט זהב (תואם ל-premium-electric הקיים), e-commerce נקי.
- **טיפוגרפיה:** Rubik / Nunito Sans (ידידותי לעברית) או הפונטים הקיימים בדמו אם טובים.
- אפקטים 150-300ms, כרטיסי glass, reveal מדורג. אייקונים Lucide (לא אמוג'י). RTL, נגיש, רספונסיבי.

## ארכיטקטורה ופיזור (פרויקט גדול — מפזרים)
### שלב 1 — בונה-החבילות + תמחור (הליבה, ניתן-להדגמה)
- עמוד/סקשן חדש `PackageBuilder` על premium-electric.
- **4 קלטים:** רמת-מותג (מעוצב / פרמיום / תקציב — בורר טאב/צ'יפ), תקציב (סליידר/מספר),
  סוגי מכשירים (בחירה מרובה: מקרר/תנור/מכונת כביסה/מדיח/...).
- **מנוע המלצה** (Cloudflare Function `functions/api/recommend.js`): לפי תקציב+רמה+סוגים,
  שואב מ-DB, בוחר מוצר מיטבי לכל סוג בתוך התקציב (אלוקטור greedy/אילוצים), וקורא ל-Groq
  לעצת שדרוג. מחזיר את החבילה.
- **לוגיקת תמחור** (util משותף `src/utils/pricing.js`): single = round(zapLow×0.95);
  package = round(sum(zapLow)×0.90). מציג מחיר-בודד פר-מוצר + סה"כ-חבילה (החיסכון מודגש:
  מול בודדים ומול זאף).
- **תוצאות UI:** כרטיסי מוצר (מחיר/מפרט/תמונה) + סיכום-חבילה דביק (סה"כ + חיסכון).
- **מקור נתונים לשלב 1:** seed ב-Firestore (או הרחבת `src/data/products.js` הקיים עם
  `brandTier`+`zapLow`) כדי שהבונה יעבוד עוד לפני שהפאנל קיים.

### שלב 2 — פאנל קליטת מוצרים (מנהל בלבד, נעול-אימות)
- העלאת **Excel** (פרסור שורות ישיר) / **PDF**/**Word** (חילוץ ע"י Groq) / **כתובת אתר**
  (fetch+חילוץ). `functions/api/ingest.js`.
- חילוץ → רשימת מוצרים מנורמלת (שם, דגם, קטגוריה, מותג, רמה, מפרט, תמונה).
- **טבלת סקירה:** כל מוצר עם checkbox (הוסף/דלג) + שדות לעריכה + מחיר-זאף (שלב 3 ממלא).
- הנבחרים → נשמרים ל-Firestore `products`.

### שלב 3 — שליפת מחיר-זאף היברידית (הכי רגיש, אחרון)
- Function `functions/api/zap-lookup.js`: לפי שם/דגם → מחפש ב-zap.co.il, מחלץ את המחיר הנמוך.
- ⚠️ **לזאף אין API רשמי; scraping שביר + סיכון ToS/משפטי.** היברידי: ניסיון-אוטו, מציג
  למנהל לאישור/תיקון; הזנה ידנית תמיד זמינה כ-fallback. אם זאף חוסם/נשבר — האפליקציה
  ממשיכה לעבוד עם מחירים ידניים. (לסמן: לשקול חוקיות / מקורות-מחיר רשמיים בהמשך.)

## קבצים
- בונים ב-`~/Private/premium-electric` הקיים. חדש: `src/pages/PackageBuilderPage.jsx`,
  `src/components/builder/*`, `src/pages/admin/*`, `functions/api/{recommend,ingest,zap-lookup}.js`,
  `src/utils/pricing.js`, סכמת Firestore `products`. שימוש חוזר ב-ProductCard/Navbar/Hero/
  AIChatBot + tokens מ-`tailwind.config.js` (זהב+שחור). הגדרת Firebase + Storage להעלאות.

## אימות (end-to-end)
1. שלב 1: seed כמה מוצרים → להריץ את הבונה (תקציב+סוגים) → בחירות נכונות בתוך התקציב,
   תמחור single/package נכון, עצת שדרוג מופיעה. `npm run dev` + ויזואלי.
2. שלב 2: להעלות Excel/PDF לדוגמה → מוצרים מחולצים → טבלת סקירה → שמירה → מופיעים ב-DB+בונה.
3. שלב 3: zap-lookup על מוצר ידוע → מחזיר מחיר (או fallback ידני חלק). לוודא שעריכה ידנית עובדת.
4. דיפלוי: Cloudflare Pages (כמו fire-safety-landing). פרויקט Firebase מוגדר.

## פיזור (כל שלב נשלח/נבדק בנפרד)
שלב 1 (בונה+תמחור) הוא הלב שאפשר להראות מהר → שלב 2 (קליטה) → שלב 3 (זאף, עם fallback ידני).

## מחוץ לסקופ (לעת עתה)
- צ'קאאוט/תשלום אמיתי (הבונה מציג תמחור; קנייה בפועל בשלב מאוחר).
- אמינות-מלאה של אוטומציית זאף (ה-fallback הידני הוא רשת הביטחון).

## לאישור/השלמה לפני בנייה
- שם/מיתוג העסק (ברירת מחדל: Premium Electric, זהב+שחור).
- פרויקט Firebase + מפתח Groq + חשבון Cloudflare.
- היכן לבנות/לדפלוי (Mac `~/Private/premium-electric` מול השרת).
